<section>
  <h2>Find a movie</h2>
  <form action="/ui/movie/show" method="get">
    <input type="text" name="id" placeholder="_id (24-hex)">
    <input type="number" name="movie_id" placeholder="movie_id">
    <button type="submit">Show</button>
  </form>
</section>

<section>
  <h2>All Movies (first 50)</h2>
  {{#if movies.length}}
    <ul>
      {{#each movies}}
        <li>
          <strong>{{movie_title}}</strong>
          — id: {{_id}} — movie_id: {{movie_id}} — Released: {{Released}}
        </li>
      {{/each}}
    </ul>
  {{else}}
    <p>No movies found.</p>
  {{/if}}
</section>

{{!-- Debug: shows the first raw document so you can see real keys --}}
<!-- your list above here -->

{{#if showDebug}}
<section>
  <h3>Debug (first raw doc)</h3>
  <pre>{{json sample}}</pre>
</section>
{{/if}}
